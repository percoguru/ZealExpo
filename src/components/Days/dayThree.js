import React, { Component } from 'react';
import { ScrollView, Button, View, Text, TouchableNativeFeedback, Platform } from 'react-native';




class DayThree extends Component{
    render() {
        return (
        <ScrollView>
          <Text style={{fontSize:50}}>Day 3</Text>
        </ScrollView>
        );
    }
};

export default DayThree;