import React, { Component } from 'react';
import { ScrollView, Button, View, Text, TouchableNativeFeedback, Platform } from 'react-native';




class DayTwo extends Component{
    render() {
        return (
        <ScrollView>
          <Text style={{fontSize:50}}>Day 2</Text>
        </ScrollView>
        );
    }
};

export default DayTwo;